<div class="getnew-container">
  <h3 class="getnew">Lisää tai poista varauksia <%= link_to "täältä.", uusivaraus_path(user_id: current_user.id, reservation_target_id: @target.id) , class: "text-link"%></h3>
</div>
